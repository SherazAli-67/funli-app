import 'package:flutter/cupertino.dart';

class RemoteUserBookmarkWidget extends StatelessWidget{
  const RemoteUserBookmarkWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Remote user bookmark page"),);
  }

}