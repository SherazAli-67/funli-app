import 'package:flutter/material.dart';

class EditFeelPage extends StatelessWidget{
  const EditFeelPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Column(
        children: [
          Text("Welcome to Edit Feel Page")
        ],
      )),
    );
  }

}